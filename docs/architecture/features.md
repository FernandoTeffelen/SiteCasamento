# Organização por funcionalidade

Cada domínio novo terá uma pasta própria aqui, por exemplo `event`, `guest`,
`missions`, `submissions` ou `ranking`. Componentes e lógica específicos não
devem ser colocados diretamente em `app/`.

